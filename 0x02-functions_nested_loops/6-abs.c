#include "main.h"

/**
 * _abs(int) - computes the absolute value of an integer
 *
 * @a: an intiger under investigation
 *
 * Return: absolute value of int
 */
int _abs(int a)
{
	if (a >=0 )
	{
		return (a);
	}
	return (-a);
}
