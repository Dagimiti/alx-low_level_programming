#include "main.h"

/**
 * mul - multiplies two intigers
 * @a: intiger one
 * @b: intiger two
 *
 * Return: result of the multiplication
 */

int mul(int a, int b)
{
	int multiply;

	multiply = (a * b);
	return (multiply);
}
